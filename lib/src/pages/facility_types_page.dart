import 'package:flutter/material.dart';

class FacilityTypesPage extends StatelessWidget {
  const FacilityTypesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Facility Types'),
      ),
      body: Center(
        child: Text('Welcome to Facility Types Page!'),
      ),
    );
  }
}
